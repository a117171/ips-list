name: 更新IP库
on:
  workflow_dispatch:
  schedule:
    - cron: '30 21 * * *'  # 每天的8点、16点执行（UTC时间）

permissions:
  contents: write

jobs:
  build1:
    runs-on: ubuntu-latest
    steps:
      - name: 检出代码仓库
        uses: actions/checkout@v4

      - name: Check PROXYIP_URL variable
        run: |
          # 检查是否设置了PROXIP_URL变量
          PROXIP_URL="${{ vars.PROXIP_URL }}"
          if [ -z "$PROXIP_URL" ]; then
            echo "❌ 错误: 未设置 PROXIP_URL 变量"
            echo "请在仓库设置中添加 PROXIP_URL 变量："
            echo "1. 进入仓库 Settings > Secrets and variables > Actions"
            echo "2. 在 Variables 标签页中添加新变量"
            echo "3. Name: PROXIP_URL"
            echo "4. Value: 脚本的URL地址"
            exit 1
          fi
          echo "✅ PROXIP_URL 变量已设置: $PROXIP_URL"

      - name: Check URL status
        id: check_url
        run: |
          STATUS_CODE=$(curl -s -o /dev/null -w "%{http_code}" "${{ vars.PROXIP_URL }}")
          echo "Status code: $STATUS_CODE"
          if [ "$STATUS_CODE" = "200" ]; then
            echo "url_status_ok=true" >> $GITHUB_OUTPUT
            echo "URL 响应正常，状态码为 200"
          else
            echo "url_status_ok=false" >> $GITHUB_OUTPUT
            echo "URL 响应异常，状态码为 $STATUS_CODE，将跳过后续步骤"
          fi
      
      - name: Download
        run: |
          curl -L "${{ vars.PROXIP_URL }}" -o proxyip.txt

      - name: 提交更新的反代IP库文件
        run: |
          git config --local user.email "action@github.com"
          git config --local user.name "GitHub Action"
          
          # 检查文件是否有变化
          if [ ! -f proxyip.txt ] || [ ! -s proxyip.txt ]; then
            echo "proxyip.txt 文件不存在或为空，跳过提交"
            exit 0
          fi
          
          if git diff --quiet proxyip.txt 2>/dev/null; then
            echo "proxyip.txt 文件没有变化，跳过提交"
          else
            echo "检测到 proxyip.txt 文件有变化，开始提交"
            git add proxyip.txt
            git commit -m "更新反代IP库文件 $(date '+%Y-%m-%d %H:%M:%S')"
            git push
            echo "提交完成"
          fi
  build2:
    runs-on: ubuntu-latest
    steps:
      - name: 检出代码仓库
        uses: actions/checkout@v4

      - name: Check SubsCheck_URL variable
        run: |
          # 检查是否设置了SubsCheck_URL变量
          SubsCheck_URL="${{ vars.SubsCheck_URL }}"
          if [ -z "$SubsCheck_URL" ]; then
            echo "❌ 错误: 未设置 SubsCheck_URL 变量"
            echo "请在仓库设置中添加 SubsCheck_URL 变量："
            echo "1. 进入仓库 Settings > Secrets and variables > Actions"
            echo "2. 在 Variables 标签页中添加新变量"
            echo "3. Name: SubsCheck_URL"
            echo "4. Value: 脚本的URL地址"
            exit 1
          fi
          echo "✅ SubsCheck_URL 变量已设置: $SubsCheck_URL"

      - name: Check URL status
        id: check_url
        run: |
          STATUS_CODE=$(curl -s -o /dev/null -w "%{http_code}" "${{ vars.SubsCheck_URL }}")
          echo "Status code: $STATUS_CODE"
          if [ "$STATUS_CODE" = "200" ]; then
            echo "url_status_ok=true" >> $GITHUB_OUTPUT
            echo "URL 响应正常，状态码为 200"
          else
            echo "url_status_ok=false" >> $GITHUB_OUTPUT
            echo "URL 响应异常，状态码为 $STATUS_CODE，将跳过后续步骤"
          fi
      
      - name: Download
        run: |
          curl -L "${{ vars.SubsCheck_URL }}" -o SubsCheck.txt

      - name: 提交更新的SubsCheckIP库文件
        run: |
          git config --local user.email "action@github.com"
          git config --local user.name "GitHub Action"
          
          # 检查文件是否有变化
          if [ ! -f SubsCheck.txt ] || [ ! -s SubsCheck.txt ]; then
            echo "SubsCheck.txt 文件不存在或为空，跳过提交"
            exit 0
          fi
          
          if git diff --quiet SubsCheck.txt 2>/dev/null; then
            echo "SubsCheck.txt 文件没有变化，跳过提交"
          else
            echo "检测到 SubsCheck.txt 文件有变化，开始提交"
            git add SubsCheck.txt
            git commit -m "更新SubsCheckIP库文件 $(date '+%Y-%m-%d %H:%M:%S')"
            git push
            echo "提交完成"
          fi
  build3:
    runs-on: ubuntu-latest
    steps:
      - name: 检出代码仓库
        uses: actions/checkout@v4

      - name: Check CIDR_URL variable
        run: |
          # 检查是否设置了CIDR_URL变量
          CIDR_URL="${{ vars.CIDR_URL }}"
          if [ -z "$CIDR_URL" ]; then
            echo "❌ 错误: 未设置 CIDR_URL 变量"
            echo "请在仓库设置中添加 CIDR_URL 变量："
            echo "1. 进入仓库 Settings > Secrets and variables > Actions"
            echo "2. 在 Variables 标签页中添加新变量"
            echo "3. Name: CIDR_URL"
            echo "4. Value: 脚本的URL地址"
            exit 1
          fi
          echo "✅ CIDR_URL 变量已设置: $CIDR_URL"

      - name: Check URL status
        id: check_url
        run: |
          STATUS_CODE=$(curl -s -o /dev/null -w "%{http_code}" "${{ vars.CIDR_URL }}")
          echo "Status code: $STATUS_CODE"
          if [ "$STATUS_CODE" = "200" ]; then
            echo "url_status_ok=true" >> $GITHUB_OUTPUT
            echo "URL 响应正常，状态码为 200"
          else
            echo "url_status_ok=false" >> $GITHUB_OUTPUT
            echo "URL 响应异常，状态码为 $STATUS_CODE，将跳过后续步骤"
          fi
      
      - name: Download
        run: |
          curl -L "${{ vars.CIDR_URL }}" -o CIDR.txt

      - name: 提交更新的CIDRIP库文件
        run: |
          git config --local user.email "action@github.com"
          git config --local user.name "GitHub Action"
          
          # 检查文件是否有变化
          if [ ! -f CIDR.txt ] || [ ! -s CIDR.txt ]; then
            echo "CIDR.txt 文件不存在或为空，跳过提交"
            exit 0
          fi
          
          if git diff --quiet CIDR.txt 2>/dev/null; then
            echo "CIDR.txt 文件没有变化，跳过提交"
          else
            echo "检测到 CIDR.txt 文件有变化，开始提交"
            git add CIDR.txt
            git commit -m "更新CFIP库文件 $(date '+%Y-%m-%d %H:%M:%S')"
            git push
            echo "提交完成"
          fi
  build4:
    runs-on: ubuntu-latest
    steps:
      - name: 检出代码仓库
        uses: actions/checkout@v4

      - name: Check CIDR_ALL_URL variable
        run: |
          # 检查是否设置了CIDR_ALL_URL变量
          CIDR_ALL_URL="${{ vars.CIDR_ALL_URL }}"
          if [ -z "$CIDR_ALL_URL" ]; then
            echo "❌ 错误: 未设置 CIDR_ALL_URL 变量"
            echo "请在仓库设置中添加 CIDR_ALL_URL 变量："
            echo "1. 进入仓库 Settings > Secrets and variables > Actions"
            echo "2. 在 Variables 标签页中添加新变量"
            echo "3. Name: CIDR_ALL_URL"
            echo "4. Value: 脚本的URL地址"
            exit 1
          fi
          echo "✅ CIDR_ALL_URL 变量已设置: $CIDR_ALL_URL"

      - name: Check URL status
        id: check_url
        run: |
          STATUS_CODE=$(curl -s -o /dev/null -w "%{http_code}" "${{ vars.CIDR_ALL_URL }}")
          echo "Status code: $STATUS_CODE"
          if [ "$STATUS_CODE" = "200" ]; then
            echo "url_status_ok=true" >> $GITHUB_OUTPUT
            echo "URL 响应正常，状态码为 200"
          else
            echo "url_status_ok=false" >> $GITHUB_OUTPUT
            echo "URL 响应异常，状态码为 $STATUS_CODE，将跳过后续步骤"
          fi
      
      - name: Download
        run: |
          curl -L "${{ vars.CIDR_ALL_URL }}" -o CIDR_ALL.txt

      - name: 提交更新的CIDR_ALLIP库文件
        run: |
          git config --local user.email "action@github.com"
          git config --local user.name "GitHub Action"
          
          # 检查文件是否有变化
          if [ ! -f CIDR_ALL.txt ] || [ ! -s CIDR_ALL.txt ]; then
            echo "CIDR_ALL.txt 文件不存在或为空，跳过提交"
            exit 0
          fi
          
          if git diff --quiet CIDR_ALL.txt 2>/dev/null; then
            echo "CIDR_ALL.txt 文件没有变化，跳过提交"
          else
            echo "检测到 CIDR_ALL.txt 文件有变化，开始提交"
            git add CIDR_ALL.txt
            git commit -m "更新CFIP库文件 $(date '+%Y-%m-%d %H:%M:%S')"
            git push
            echo "提交完成"
          fi
  build5:
    runs-on: ubuntu-latest
    steps:
      - name: 检出代码仓库
        uses: actions/checkout@v4

      - name: Check SOCKS5_URL variable
        run: |
          # 检查是否设置了SOCKS5_URL变量
          SOCKS5_URL="${{ vars.SOCKS5_URL }}"
          if [ -z "$SOCKS5_URL" ]; then
            echo "❌ 错误: 未设置 SOCKS5_URL 变量"
            echo "请在仓库设置中添加 SOCKS5_URL 变量："
            echo "1. 进入仓库 Settings > Secrets and variables > Actions"
            echo "2. 在 Variables 标签页中添加新变量"
            echo "3. Name: SOCKS5_URL"
            echo "4. Value: 脚本的URL地址"
            exit 1
          fi
          echo "✅ SOCKS5_URL 变量已设置: $SOCKS5_URL"

      - name: Check URL status
        id: check_url
        run: |
          STATUS_CODE=$(curl -s -o /dev/null -w "%{http_code}" "${{ vars.SOCKS5_URL }}")
          echo "Status code: $STATUS_CODE"
          if [ "$STATUS_CODE" = "200" ]; then
            echo "url_status_ok=true" >> $GITHUB_OUTPUT
            echo "URL 响应正常，状态码为 200"
          else
            echo "url_status_ok=false" >> $GITHUB_OUTPUT
            echo "URL 响应异常，状态码为 $STATUS_CODE，将跳过后续步骤"
          fi
      
      - name: Download
        run: |
          curl -L "${{ vars.SOCKS5_URL }}" -o SOCKS5.txt

      - name: 提交更新的SOCKS5IP库文件
        run: |
          git config --local user.email "action@github.com"
          git config --local user.name "GitHub Action"
          
          # 检查文件是否有变化
          if [ ! -f SOCKS5.txt ] || [ ! -s SOCKS5.txt ]; then
            echo "SOCKS5.txt 文件不存在或为空，跳过提交"
            exit 0
          fi
          
          if git diff --quiet SOCKS5.txt 2>/dev/null; then
            echo "SOCKS5.txt 文件没有变化，跳过提交"
          else
            echo "检测到 SOCKS5.txt 文件有变化，开始提交"
            git add SOCKS5.txt
            git commit -m "更新SOCKS5IP库文件 $(date '+%Y-%m-%d %H:%M:%S')"
            git push
            echo "提交完成"
          fi
